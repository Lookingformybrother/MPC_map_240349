function [estimated_pose] = estimate_pose(read_only_vars, public_vars)
%ESTIMATE_POSE Summary of this function goes here
estimated_pose=read_only_vars.mocap_pose; %pouze pro toto cvičení
%estimated_pose = nan(1,3);

end

